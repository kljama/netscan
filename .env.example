# .env.example - Environment variables for Docker Compose deployment
#
# USAGE:
# 1. Copy this file: cp .env.example .env
# 2. Update the values below with your production credentials
# 3. Keep .env file secure (it's in .gitignore)
#
# These environment variables will be used by docker-compose.yml to configure
# both the netscan and InfluxDB services.

# =============================================================================
# NETSCAN ENVIRONMENT VARIABLES
# =============================================================================
# These are passed to the netscan container and expanded in config.yml

# InfluxDB connection token (should match DOCKER_INFLUXDB_INIT_ADMIN_TOKEN below)
INFLUXDB_TOKEN=netscan-token

# InfluxDB organization (should match DOCKER_INFLUXDB_INIT_ORG below)
INFLUXDB_ORG=test-org

# SNMP community string for device authentication
# IMPORTANT: Change from default 'public' for production!
SNMP_COMMUNITY=public

# =============================================================================
# INFLUXDB ENVIRONMENT VARIABLES
# =============================================================================
# These configure the InfluxDB container initialization

# InfluxDB initialization mode
DOCKER_INFLUXDB_INIT_MODE=setup

# InfluxDB admin credentials
DOCKER_INFLUXDB_INIT_USERNAME=admin
DOCKER_INFLUXDB_INIT_PASSWORD=admin123

# InfluxDB organization (should match INFLUXDB_ORG above)
DOCKER_INFLUXDB_INIT_ORG=test-org

# InfluxDB bucket for metrics storage
DOCKER_INFLUXDB_INIT_BUCKET=netscan

# Data retention period
DOCKER_INFLUXDB_INIT_RETENTION=1w

# InfluxDB API token (should match INFLUXDB_TOKEN above)
DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=netscan-token
